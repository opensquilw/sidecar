/* Deployment config. Set AI_ENDPOINT to your deployed worker URL
   (see worker/README.md) and every install gets the AI assistant.
   Leave empty and the assistant runs in offline guide mode. */
const AI_ENDPOINT = "";
